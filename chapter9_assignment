def mirror(text):
    full_text = text + reverse(text)
    return full_text


def reverse(text):
    new_text = ""
    for char in text:
        new_text = char + new_text
    return new_text
